import React from 'react'; import {Link,useNavigate} from 'react-router-dom'; import {Users,GraduationCap,Building2,FlaskConical,Rocket,Landmark} from 'lucide-react'; import {saveUser} from '../lib';
const roles=[[Users,'Citizen / Community'],[GraduationCap,'Student'],[Building2,'University / HEI'],[FlaskConical,'Researcher'],[Building2,'Industry'],[Rocket,'Startup'],[Landmark,'Government']];
export default function Register(){const nav=useNavigate(); const [role,setRole]=React.useState('Student'); const [name,setName]=React.useState(''); const [email,setEmail]=React.useState(''); const submit=e=>{e.preventDefault(); if(!name||!email)return; saveUser({name,email,role});nav('/dashboard')}; return <section className="auth-page"><form className="register-card" onSubmit={submit}><span className="eyebrow">JOIN THE ECOSYSTEM</span><h1>How would you like to participate?</h1><p>Select a role and create your local demo account.</p><div className="role-grid">{roles.map(([I,t])=><button type="button" className={role===t?'role-card selected':'role-card'} key={t} onClick={()=>setRole(t)}><I/><b>{t}</b></button>)}</div><div className="two-col"><label>Name<input value={name} onChange={e=>setName(e.target.value)} placeholder="Your name"/></label><label>Email<input value={email} onChange={e=>setEmail(e.target.value)} type="email" placeholder="you@example.com"/></label></div><div className="form-actions"><Link to="/login" className="outline-btn">Already have an account?</Link><button type="submit" className="primary-btn">Create account</button></div></form></section>}
