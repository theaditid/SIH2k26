import React from 'react'; import {Search} from 'lucide-react'; import {SolutionCard} from '../components/UI'; import {getSolutions} from '../lib';
export default function Solutions(){const data=getSolutions();const [q,setQ]=React.useState('');const [cat,setCat]=React.useState('All');const filtered=data.filter(x=>(cat==='All'||x.category===cat)&&(!q||`${x.title} ${x.org} ${x.category}`.toLowerCase().includes(q.toLowerCase())));return <><section className="page-hero"><span className="eyebrow light">FROM IDEAS TO IMPACT</span><h1>Innovative Solutions</h1><p>Discover ideas, prototypes, pilots and implemented solutions created by the Innova8 ecosystem.</p></section><section className="section"><form className="searchbar" onSubmit={e=>e.preventDefault()}><Search/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search solutions by technology, category or partner"/><button className="primary-btn">Search</button></form><div className="chip-row">{['All',...new Set(data.map(x=>x.category))].map(x=><button className={cat===x?'active':''} onClick={()=>setCat(x)} key={x}>{x}</button>)}</div><div className="card-grid three">{filtered.map(s=><SolutionCard key={s.id} {...s}/>)}</div></section></>}
